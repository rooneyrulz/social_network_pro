// GLOBAL STYLES GO ON GERE
import {
  Platform,
  StyleSheet,
  StatusBar as NativeStatusBar,
} from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
  },
  text: {
    // color: '#000',
    fontSize: 20,
  },
});
